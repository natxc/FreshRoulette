{
  "name": "fresh-roulette-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.4",
    "express": "^4.21.2",
    "pg": "^8.11.3",
    "react-beautiful-dnd": "^13.1.1"
  },
  "engines": {
    "node": ">=14.x"
  }
}
